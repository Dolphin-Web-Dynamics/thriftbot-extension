name: Publish to Chrome Web Store

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      mode:
        description: "Upload mode"
        required: true
        type: choice
        options:
          - draft
          - publish

permissions:
  contents: write

concurrency:
  group: chrome-web-store
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.meta.outputs.version }}
      zip-name: ${{ steps.meta.outputs.zip-name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript compile check
        run: npm run compile

      - name: Read version from package.json
        id: meta
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "zip-name=thriftbot-extension-${VERSION}-chrome.zip" >> "$GITHUB_OUTPUT"

      - name: Validate release tag matches package.json version
        if: github.event_name == 'release'
        run: |
          TAG="${{ github.event.release.tag_name }}"
          EXPECTED="v${{ steps.meta.outputs.version }}"
          if [ "$TAG" != "$EXPECTED" ]; then
            echo "::error::Release tag $TAG does not match package.json version $EXPECTED"
            exit 1
          fi

      - name: Build and zip extension
        run: npm run zip

      - name: Upload zip artifact
        uses: actions/upload-artifact@v6
        with:
          name: chrome-extension-zip
          path: .output/${{ steps.meta.outputs.zip-name }}
          if-no-files-found: error
          retention-days: 90

  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest

    outputs:
      created: ${{ steps.check.outputs.created }}

    steps:
      - name: Check if release already exists
        id: check
        run: |
          VERSION="v${{ needs.build.outputs.version }}"
          if gh release view "$VERSION" --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1; then
            echo "Release $VERSION already exists — skipping"
            echo "created=false" >> "$GITHUB_OUTPUT"
          else
            echo "Creating release $VERSION"
            gh release create "$VERSION" --repo "$GITHUB_REPOSITORY" --generate-notes
            echo "created=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  upload-draft:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.mode == 'draft'
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Download zip artifact
        uses: actions/download-artifact@v7
        with:
          name: chrome-extension-zip
          path: .output

      - name: Check CWS status
        run: bash scripts/check-cws-status.sh
        env:
          CHROME_EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          CHROME_CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}

      - name: Upload draft to Chrome Web Store
        run: npm run submit -- --chrome-zip .output/${{ needs.build.outputs.zip-name }} --chrome-skip-submit-review
        env:
          CHROME_EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          CHROME_CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}

  publish:
    if: >-
      always() &&
      needs.build.result == 'success' &&
      (
        needs.release.outputs.created == 'true' ||
        github.event_name == 'release' ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.mode == 'publish')
      )
    needs: [build, release]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Download zip artifact
        uses: actions/download-artifact@v7
        with:
          name: chrome-extension-zip
          path: .output

      - name: Check CWS status
        run: bash scripts/check-cws-status.sh
        env:
          CHROME_EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          CHROME_CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}

      - name: Submit to Chrome Web Store for review
        run: npm run submit -- --chrome-zip .output/${{ needs.build.outputs.zip-name }}
        env:
          CHROME_EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          CHROME_CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}

      - name: Attach zip to GitHub Release
        if: github.event_name == 'release' || needs.release.outputs.created == 'true'
        run: |
          TAG="${{ github.event.release.tag_name || format('v{0}', needs.build.outputs.version) }}"
          gh release upload "$TAG" ".output/${{ needs.build.outputs.zip-name }}" --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
